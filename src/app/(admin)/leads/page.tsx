import { Placeholder } from "../placeholder";

export default function Page() {
  return <Placeholder title="Leads" summary="Lead list and detail, with PII masked until explicitly revealed (PRD 26.2)." arrives="Phase 1" />;
}
